---
name: "Live Channels"
guid: "ac6940fb-aac6-4de8-b622-55a662e23658"
version: "10.11.9.5"
targetAbi: "10.11.9.0"
framework: "net9.0"
overview: "Build looping virtual TV channels that appear natively in Jellyfin's Live TV."
description: >
  Live Channels turns your own library into looping virtual TV channels that play natively in
  Jellyfin's Live TV. The channels, their guide, and their streams are all served inside Jellyfin
  itself, so they appear automatically with no tuner to add, no URLs to paste, and no exposed endpoints.
category: "Live TV"
imageUrl: "https://raw.githubusercontent.com/JPKribs/jellyfin-plugin-livechannels/refs/heads/master/Jellyfin.Plugin.LiveChannels/Assets/Logo.png"
owner: "JPKribs"
artifacts:
- "Jellyfin.Plugin.LiveChannels.dll"
changelog:
  developer:
  - Dolby Vision Profile 5 items are now excluded from schedules with a clear log line - Profile 5 has no HDR10-compatible base layer, so it renders green/purple through any tone mapper; Profile 8 and HDR10 versions play correctly and stay included
  - Schedules now heal metadata runtime drift - when a full item plays through, its real length is recorded and used from the next guide refresh, closing the timestamp gap (or overlap) at item boundaries when a file's metadata runtime is wrong

