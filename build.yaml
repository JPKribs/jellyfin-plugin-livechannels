---
name: "Live Channels"
guid: "ac6940fb-aac6-4de8-b622-55a662e23658"
version: "12.0.1.1"
targetAbi: "12.0.0.0"
framework: "net10.0"
overview: "Build looping virtual TV channels that appear natively in Jellyfin's Live TV."
description: >
  Live Channels turns your own library into looping virtual TV channels that play natively in
  Jellyfin's Live TV. The channels, their guide, and their streams are all served inside Jellyfin
  itself, so they appear automatically with no tuner to add, no URLs to paste, and no exposed endpoints.
category: "Live TV"
imageUrl: "https://raw.githubusercontent.com/JPKribs/jellyfin-plugin-livechannels/refs/heads/master/Jellyfin.Plugin.LiveChannels/Assets/Logo.png"
owner: "JPKribs"
artifacts:
- "Jellyfin.Plugin.LiveChannels.dll"
changelog:
  user:
  - A channel with no viewers now stops when its countdown reaches zero. It used to run on for two to three minutes after the last viewer left
  - Tune-in cards are generated for every channel shortly after the server starts and at every guide refresh, including the refresh a save triggers, so a fresh install, an update, a new logo, or changed output settings never leave a channel opening without its card
  developer:
  - The linger teardown was vetoed every time: Jellyfin's reader is served right up to the close, so the two-minute recent-delivery rule always saw a viewer at the 30s mark, and the one-shot task then gave up until the reaper's 90s-plus-a-minute backstop. A lingering session now only counts delivery after the close (DeliveryCountsAsWatched, unit tested), and LingerTeardownAsync retries every 5s until the reaper threshold
  - LiveChannelsTvService sweeps the tune-in cards 20s after construction and from every GetChannelsAsync (EnsureLogoAsync then IntroService.EnsureAsync per enabled channel, serialised, logged with its trigger); a tune-in without a card now logs it
  - The Sessions tab countdown wording no longer uses an em dash or a semicolon
