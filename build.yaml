---
name: "Live Channels"
guid: "ac6940fb-aac6-4de8-b622-55a662e23658"
version: "10.11.9.1"
targetAbi: "10.11.9.0"
framework: "net9.0"
overview: "Build looping virtual TV channels that appear natively in Jellyfin's Live TV, with a ready-made Popular channel out of the box."
description: >
  Live Channels turns your own library into looping virtual TV channels that play natively in
  Jellyfin's Live TV. The channels, their guide, and their streams are all served inside Jellyfin
  itself, so they appear automatically with no tuner to add, no URLs to paste, and no exposed endpoints.
category: "Live TV"
imageUrl: "https://raw.githubusercontent.com/JPKribs/jellyfin-plugin-livechannels/refs/heads/master/Jellyfin.Plugin.LiveChannels/Assets/Logo.png"
owner: "JPKribs"
artifacts:
- "Jellyfin.Plugin.LiveChannels.dll"
changelog:
  developer:
  - The Sessions tab shows when a viewerless stream is winding down (with its countdown), and its Kill button now stops the encoder immediately instead of waiting out the wind-down
  - New encoder stress test on the Settings tab - pick a demanding library item and the server measures how many concurrent streams it can sustain with the real channel pipeline, recommending the Maximum concurrent streams value
  - Fully GPU-resident Intel pipeline (Linux QSV/VAAPI) - decode, deinterlace, scale, tone map, and encode all stay in VRAM on Jellyfin's configured render node. Benchmarked on an N100 iGPU with 4K HDR10 HEVC, 2.6x faster than the old download-to-CPU chain; the Intel 10-bit and interlaced software-decode workarounds are gone, and HDR tone maps scale-first (1.9x faster, same picture)
