---
name: "Live Channels"
guid: "ac6940fb-aac6-4de8-b622-55a662e23658"
version: "10.11.3.3"
targetAbi: "10.11.9.0"
framework: "net9.0"
overview: "Build looping virtual TV channels that appear natively in Jellyfin's Live TV."
description: >
  Live Channels lets administrators build looping virtual TV channels from genres and official
  ratings or from a hand-picked list of items. Each channel, its guide, and its stream are served
  in-process through Jellyfin's own Live TV, so the channels appear automatically with no tuner to
  add, no URLs to paste, and no exposed endpoints.
category: "Live TV"
imageUrl: "https://raw.githubusercontent.com/JPKribs/jellyfin-plugin-livechannels/refs/heads/master/Jellyfin.Plugin.LiveChannels/Assets/Logo.png"
owner: "JPKribs"
artifacts:
- "Jellyfin.Plugin.LiveChannels.dll"
changelog:
  features:
  - Exclude genres from a channel, with series level genres applied across their episodes
  - Limit a channel to a single audio language by its default audio track
  - Set the playback buffer length and the stream file location in Settings
  - Clean up leftover stream files with a scheduled task in Live TV
  fixes:
  - Stream channels as a rolling playlist of small segments so disk use stays bounded no matter how long they run
  - Pre-roll a few seconds on tune-in so playback starts on a full buffer
  - Tidy the channel editor genre dropdown and favor layout
